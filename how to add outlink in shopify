# how to add outlink in product page use tag
# like amzon url add in product page
 
 
 {% for tag in product.tags %} 
              {%if tag contains 'url_'  %}
              {% assign content = tag | split: 'url_' %}
               <a href="{{ content }}"> buy form amazon </a>
              {% endif %}
           {% endfor %}
              
              
